# Improvement ideas

- optimize hyperparameters (use `optuna` or `hyperopt` for example)
- incorporate non-linear parameter reduction techniques (e.g. `PCA` or `t-SNE`)
- use other models: SVM, LightGBM, CNN on spectrograms
- find better model for music embedding (e.g. `Laion CLAP` - hugging face)
